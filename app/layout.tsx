export const metadata = {
  title: 'GitHub Profile',
  description: 'A personal GitHub profile page',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
